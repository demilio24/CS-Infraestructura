# Secrets — never commit, ever (public repo)
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
*.pem
*.key
**/credentials.json
**/secrets.json
**/.credentials.json
**/oauth-token.json
**/.gh-token

# All tooling lives in .claude/ — ignore its local artifacts
.claude/.env
.claude/node_modules/
.claude/package-lock.json
.claude/screenshots/
.claude/qa-screenshots/

# Uploaded images (transient)
uploads/*
!uploads/.gitkeep

# QA test harnesses (local-only)
.claude/qa-systema-*.js
.claude/qa-*.js
.claude/screenshot-*.js
.claude/scripts/

# Claude scratch + session files (local-only)
.claude/tmp/
.claude/scratch/
.claude/.claude/
.claude/.puppeteer-userdata/
.claude/.puppeteer-deeplink-tmp/
**/scheduled_tasks.lock

# Per-project .claude/ folders (local agent state — settings.local.json, etc.)
*/.claude/

# Brainstorming companion state (superpowers:brainstorming visual companion)
.superpowers/

# Raw GHL form-submission exports (PII, not project code)
Tom_Systema_Floyd/Form Submission.csv

# Tom's seminar source design files (373MB of PSDs/ZIPs — page uses GHL CDN copies)
Tom_Systema_Floyd/Vlad Seminar/

# clasp / Apps Script local credentials (NEVER commit)
**/.clasprc-creds.json
.clasprc.json
**/.clasp.local.json
