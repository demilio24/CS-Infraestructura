# Systema Floyd — martial arts school dashboard + billing system + funnel

## What this is
Tom runs Systema Floyd, a Florida-based martial arts school (Upper Campus + Lower Campus, summer camps, after-school, spirit dance, birthday parties). This is the largest multi-system client in the repo. The folder contains: (1) a registrations/lunches **dashboard** fed by Google Sheets → Apps Script → snapshot.json on a 5-min cron, (2) a **billing dashboard** (separate Apps Script project, bound to a Sheet) that polls GHL submissions + accepts manual entries and is the canonical billing record, (3) a 11-page client-facing **funnel**, (4) the legacy site mirror in `OG_Site/`, and (5) staged docs/specs. A **registration migration** from Tom's old paper/manual system into the new auto sheets is mid-flight (29 kids open, waiting on team reply).

## Architecture

### Top-level folders
- `App_documentation/` — Markdown specs for each subsystem: `billing_dashboard.md`, `dashboard.md`, `registration_system.md`, `camp_day_validator.md`, `waiver_matcher.md`, `school_enrollment_router.md`, `new_forms_spec.md`, `forms_todos.md`. Read these for system-level intent.
- `Billing dashboard/` — The billing Apps Script project (`apps-script/`, clasp-managed, bound to the Dashboard Sheet) plus `old-registrations/` (12 weekly CSVs from Tom + `_DISCREPANCY-REPORT.md` + `_EMAIL-TO-TOM.md` intake), `prompts/`, `docs/`, `setup-gcp.md`, `REPORT.md`, `TODO.md`. Files in `apps-script/`: `Polling.js`, `Triggers.js`, `SheetWrites.js`, `Transactions.js`, `Helpers.js`, `Configuration.js`, `FieldRegistry.js`, `LegacyImport.js` (not present — was Dedup.js/LegacyImport.js earlier; current set includes `BillingFromSheets.js`, `Code.js`, `Menu.js`, `Notifications.js`, `Payments.js`, `PricingGuide.js`, `QA.js`, `RemoteTrigger.js`, `Webhook.js`).
- `OG_Site/` — Static HTML scrape of the original systemafloyd.com (After School, Camps, Contact, Spirit Dance, Program Survey, FORM, asset folders, screenshots). Reference only.
- `billing-stages/` — Staged build plan + reference docs: `00-README.md` through `06-migrate-to-polling.md`, `billing-dashboard-plan.md`, `pricing-syntax.md`. Historical narrative of how the billing system was built.
- `context/` — Active handoff notes. Currently: `2026-05-07-dashboard-handoff.md` (registrations dashboard state). Always read this folder first when resuming.
- `dashboard/` — The registrations/lunches dashboard the team actually uses. `index.html` (KPIs + roster), `lunches.html` (kitchen prep), `snapshot.json` (read by both pages, written by GH Actions cron), `history/` (daily snapshots `2026-05-01.json`..today + `index.json`).
- `funnel/` — 12 client-facing pages: `home.html`, `camps.html`, `summer-camp-scholarship.html`, `after-school.html`, `private-lessons.html`, `spirit-dance.html`, `birthday-parties.html`, `rent-a-sensei.html`, `waiver.html`, `waiver-free.html`, `thankyou.html`, `vlad-seminar.html`. All seminar imagery + the flyer PDF live in the Floyd FL GHL media library (uploaded 2026-05-17, see changelog).
- `sheets-snapshot/` — The OTHER Apps Script project (clasp-managed) — the lightweight web app the dashboard pipeline calls. `apps-script/Snapshot.js` (hardcoded SHEETS map + readers), `DiscrepancyCheck.js`, `appsscript.json`, `README.md`.

### Data flow — Registrations dashboard
4 source Sheets owned by `systemafloydsheets@gmail.com` (Upper paid, Lower paid, FREE Higher, FREE Lower) → Apps Script web app **Floyd Roster Snapshot API** (scriptId `1EcPTHTRypJX_ywQXqj_LQuJMNk2RAjzSkIxsG8QzLe64jQtRXvEf6f8Y`, pinned deployment `AKfycbw6YVSJXxwEz5UPavqQYEn_c2oFGi2-Y7Re8KlGNQytqOLeYI3k_7v85YIvuTptKI5j`) → `.claude/scripts/fetch-systema-snapshot-from-sheets.py` (called by `.github/workflows/refresh-systema-snapshot.yml` every 5 min) → writes `dashboard/snapshot.json` + daily history file → `dashboard/index.html` + `dashboard/lunches.html` read snapshot.json. Always update the pinned deployment in place: `clasp deploy --deploymentId <pinned> --description "..."`. After-school is stubbed (`emptyAfterSchool_()`); AS Drive folder `1hT0qjM_NCIkONm1-HhUr3rvB4FDV-o8V` is empty/not shared yet. See memory `project_systema_floyd_dashboard_pipeline.md`.

### Data flow — Billing dashboard (separate system)
Three ingest paths, all produce first-class tx rows on the bound Dashboard Sheet via fingerprint-based reconcile: (1) `buildAllBilling` (5-min trigger) walks the 4 registration source sheets + the hidden `Manual Items` source tab, prices each enrollment via the `Pricing` catalog, and diff-applies against the Dashboard (cancel/refund-needed lifecycle by fingerprint). (2) `pollFloridaSubmissions` (5-min trigger, legacy) still runs but only for the $1 CC-verification fee row; everything else now flows from sheet sources. (3) **Manual line items** — operator clicks `Add Item → + New manual item` (custom menu re-added 2026-05-17), a sidebar dialog (`AddManualItem.html`) writes a row to the hidden `Manual Items` tab with a stable UUID fingerprint, a one-time trigger fires `buildAllBilling` ~10s later so the row lands on the Dashboard within seconds. Direct edits to Dashboard tx rows are NOT a supported entry path — `nuclearResetBilling` wipes them. Transactions tab auto-syncs from each subaccount's `/payments/transactions`. As of 2026-05-07 baseline: 80 customers, ~634 rows, $61,104.25 balance after dedup.

### Source of truth
**Both dashboards: the Google Sheet is canonical.** Manual entries and legacy imports are equal to GHL-originated rows. Don't add features that require a `submission_id` on every row (manual rows don't have one). When pushing data back to GHL, only push rows with a `submission_id`. See memory `project_systema_floyd_sheet_source_of_truth.md`.

### Tech stack
HTML/CSS/JS dashboards (vanilla, no framework), two Google Apps Script projects (clasp-managed, bound and standalone), Google Sheets storage, GHL for forms + media + contacts, n8n for ancillary automations, Python fetcher in GitHub Actions cron, Supabase `public.ghl_tokens` for GHL OAuth (filter `account_name ILIKE '%Systema Floyd%'`, use `acces_token` column — typo is intentional).

## Conventions
- **Floyd team-facing emails go from `systemafloyd@gmail.com`** — NOT `emilio@nilsdigital.com`, NOT `systemafloydsheets@gmail.com` (that one is the Drive/Sheets file-owner permission anchor, never a sender). If the Gmail MCP is authenticated as a different account, flag the limitation up front. See memory `feedback_floyd_team_emails_account.md`.
- Snapshot bot commits with `[skip ci]` — those are noise in `git log`. Real work commits don't have that suffix.
- Apps Script deploys: always `clasp push -f && clasp deploy --deploymentId <pinned> --description "..."`. Never `clasp deploy` alone (regenerates URL, breaks the fetcher).
- During rebase, `git checkout --ours snapshot.json` means UPSTREAM (the bot's version), not your branch. Use `--theirs` or re-run the fetcher.
- Re-running any audit / dedup / import / restore function in the billing project is safe — all idempotent (Manual Imports audit keyed on `sheetId:tab:row`).
- Hook new "global" billing maintenance into the polling tail, not per-submission.
- Source sheet IDs (hardcoded — Upper paid `1qejcgNQt3sS_UZ9Gl9Txr8TOocw3LzK5PjPICqnRrGA`, Lower paid `18A_sc917xnxYo3UQ8_cGogqg46Im6qUQlakOC9Oc-Fs`, FREE Higher `1rK4p6jS1xqSf1qNO9-3ljCRzJcUIDF87sNo_UehBWYQ`, FREE Lower `1_659v7by990V4OJMd86nBG-HUN6_AzZNOAPoQN0LMxY`).
- **Manual line items go through the `Add Item` dialog, never direct sheet edits to the Dashboard.** The dialog writes to the hidden `Manual Items` source tab; `buildAllBilling` reads from it and renders to the Dashboard with proper fingerprinting + lifecycle. Direct Dashboard edits get wiped on `nuclearResetBilling`. To audit/correct a manual row, unhide `Manual Items` (right-click any tab → Show all hidden sheets), edit there, re-hide.
- **Two domains, two pivots:** the script is owned by `systemafloydsheets@gmail.com` (Sheets/Drive permissions anchor). Google blocks deployment changes (`clasp deploy`, `clasp redeploy`, `clasp create-deployment`) from any account outside that domain — running these as `emilio@nilsdigital.com` returns "Only users in the same domain as the script owner may deploy this script." `clasp push -f` to HEAD works from either account; deployment changes must run from the owner account.

## Open threads

### Registration migration (waiting on team reply)
29 kids open: 21 Lower, 7 Upper, 1 unknown-campus. Intake email is drafted at Gmail draft `r3909052957474012439` ("Re: Summer Camp 2026 registrations, a few questions and some kids to add"). Original 23-kid intake was sent to Tom on 2026-05-06 at 18:52 (thread `19dfe98a5bcec7e4`). 4 stale drafts to manually trash (MCP lacks scope): `r-2138070793564794073`, `r-4694106148698038550`, `r2077446025218548779`, `r-2135374149152529346`. Special cases to flag: Jacob Perna age 0 (typo), Olowin twins age 2 (sheet had 3), Lotan Topor borderline (age 5), Paul McIntosh notes dedup. See memory `project_systema_floyd_migration_state.md`.

### Migration next steps (when reply lands)
Parse reply → write each kid into Upper (`1qejcgNQt3sS_UZ9Gl9Txr8TOocw3LzK5PjPICqnRrGA`) or Lower (`18A_sc917xnxYo3UQ8_cGogqg46Im6qUQlakOC9Oc-Fs`) sheet per their listed weeks (Mon-Fri checkmarks + lunch/breakfast/aftercare/shirt/notes) → upsert each parent email as GHL contact in Systema Floyd location with week-attendance tags → verify snapshot.json totals shift up → regenerate `_DISCREPANCY-REPORT.md`. Then delete stale drafts. See memory `project_systema_floyd_migration_todos.md`.

### Live billing TODOs
See memory `todos_systema_floyd_billing.md` for the prioritized list. Headlines:
- **Data-correctness (do first):** fix `upsertCustomerRow` HYPERLINK overwrite bug + `restoreLostProfileLinks()` one-shot; replace email-derived "Customer Name" placeholders with parent names; audit `IMPORT PLACEHOLDER` camp-price rows; clean double-`(+$30)` t-shirt labels.
- **Sync features:** Sheet → GHL contact upsert (so manually-imported customers get phone numbers etc. into GHL); sales tax by product type (FL 6% / GA 4% / VA 5.3% — verify with Tom, don't assume; matrix keyed on source-field name in col B note).
- **Resilience:** make `upsertCustomerRow` multi-source-aware (don't clobber manual cleanup on re-poll); add Manual Imports re-import safety via content hash; surface placeholder rows in a Dashboard filter.

### Registrations dashboard TODOs
See `context/2026-05-07-dashboard-handoff.md`. Headlines: wire up after-school once Drive folder `1hT0qjM_NCIkONm1-HhUr3rvB4FDV-o8V` populates and is shared (~20 lines in `Snapshot.js`); Free Camp wiring already live, fills automatically when form opens; KPI card styling pass pending direction choice; consider bringing back GHL `totals.contacts`/`leadOnly`/`newLast7Days`/`newLast30Days` as a hybrid pass.

### Demo-prep state snapshot (most recent prior milestone)
Full file map + shipped features from 2026-05-07 in memory `project_systema_floyd_state_snapshot.md`.

### Vlad Seminar follow-ups (open)
- **Form is a placeholder.** `funnel/vlad-seminar.html` iframes the After School Registration form (`TkioOL4IoByeHU3K2gTs`) — built before a dedicated seminar form existed. Create a GHL form with: student full name, contact email, phone, pass selection (Early 2-day $275 / Early 1-day Sat $155 / Early 1-day Sun $155 / Regular tiers / etc.), Friday Private Session opt-in (Yes/No — explain 30-pass cap and confirmation by email), marketing consent. Swap the iframe `src` form ID once it exists.
- **Page is not linked from the main funnel.** Once approved with Tom, add a header/footer entry on `home.html` and create a GHL embed snippet via `/ghl-embed`. Path will be `https://demilio24.github.io/Websites/Tom_Systema_Floyd/funnel/vlad-seminar.html`.
- **n8n token-refresh cron is dead.** `s1Z2H8GwdaU7n2kG` ("1. Updates All Tokens - 3 AM") fails with `Execution limit reached. Consider upgrading your plan` — see https://app.n8n.cloud/account/change-plan. All three Floyd tokens (FL, GA, VA) will go stale every 24h until n8n plan is upgraded or the refresh is moved off n8n. Manual refresh recipe documented in the 2026-05-17 changelog entry.

## Changelog

### 2026-05-23 — App_documentation/billing_dashboard.md updated to reflect the 4 commits from today's billing session
After the 4 billing commits landed (`e2a395e9`, `04bb2d02`, `e64beb67`, `bc835b61`), the long-form spec doc [App_documentation/billing_dashboard.md](App_documentation/billing_dashboard.md) was out of date. Three changes: (1) **§9.3 Common failures** got 4 new rows — "kids labeled under wrong week" (links to the `e2a395e9` fix), "Name/Phone keeps reverting to GHL after manual fix" (links to `04bb2d02`), "View profile link disappeared, shows '(not found in Florida)' plain text" (links to `04bb2d02` + the `restoreLostProfileLinks` recovery path), plus the existing `+/-` row was extended with a note that `fixDashboardGroups` failures now log to the Logs sheet per the `e2a395e9` instrumentation. (2) **New §9.5 "Remote-trigger webapp + RPCs"** — comprehensive doc of the webapp's auth model, sync vs scheduled invocation modes, deployment-versioning gotcha (clasp push updates HEAD but webapp runs the pinned version until you bump it; triggers DO pick up @HEAD automatically), and a full table of every whitelisted RPC including the 5 new ones from this session (`restoreLostProfileLinks`, `testUpsertPreservation`, `cleanupDoubleShirtSuffix`, `cleanupExtraNuclearTriggers`, `listAllCustomerEmails`). (3) **§10 Recovery** got a new row for reg-sheet tab reordering ("safe since e2a395e9 — tabs resolved by name via `bfsResolveWeek_`"), and **§12 Constants reference** picked up `BFS_WEEK_KEY_MAP` and `BFS_NON_BILLABLE_TYPES = ['summer-free']` (the FREE camp exclusion constant whose explicit absence was responsible for half the "missing customers" perceived discrepancy fixed in `bc835b61`). The §9.4 in-repo sample-sheet section and §9.1 editor-dropdown function table left as-is — they were already accurate.

### 2026-05-23 — Working-tree state note at end of billing session
End of the long billing-bugfix session captured in the entries below. Working tree at session close also carried uncommitted edits from other concurrent Claude sessions on this machine that this session did NOT author and intentionally did not stage: `dashboard/free-camp.html`, `dashboard/index.html`, 9× `funnel/*.html` files, and a new untracked `App_documentation/clickup_form_task_specs.md`. Those were left for their original authors to commit with proper context; only the billing-fix changes (BillingFromSheets.js, SheetWrites.js, RemoteTrigger.js, billingdashboardbugs.md, this PROJECT.md) were committed in this session's 4 commits (`e2a395e9`, `04bb2d02`, `e64beb67`, `bc835b61`). Recorded here only because the auto-memory hook flags dirty state at turn end regardless of authorship.

### 2026-05-23 — Billing reconciliation: snapshot-vs-billing audit proves no real missing contacts; 60 absences all match intentional exclusions
After fixing the +1 week shift and the three `upsertCustomerRow` bugs, user asked "do all contacts appear now?" Ran a full diff between [dashboard/snapshot.json](dashboard/snapshot.json) (registration source of truth, all 4 source sheets) and the live billing Dashboard via the remote-trigger webapp (88 sequential `traceCustomer` calls + 1 `registrationStats`). Snapshot has 121 unique parents; billing has 61 customers. The 60-customer gap breaks down entirely into intentional exclusions: **33 FREE camp parents** (filtered at `BillingFromSheets.js:254` via `BFS_NON_BILLABLE_TYPES = ['summer-free']`, with the explicit comment "those kids registered for the no-cost program and shouldn't appear with priced line items"), and **25 paid-camp parents whose every enrollment has dayCount=0** (phantom-skip at `BillingFromSheets.js:2845` — no days = nothing to bill). Of 84 paid-camp parents in the snapshot, all 59 with at least one day picked appear on billing; the other 25 are phantom-skipped. Triage rule for future "missing customer" reports captured at the top of [billingdashboardbugs.md](billingdashboardbugs.md): open the registration sheet, check if any rows have a day checked — if yes-and-not-on-billing then it's a real bug; if no, expected behavior and the team should be chasing the parent for day selection. Optional future feature noted (not a bug): surface the 25 day-zero parents on the dashboard so Tom can nudge them. Added new diagnostic RPC `listAllCustomerEmails` to RemoteTrigger.js for future re-runs (whitelisted + dispatched; will go live on next webapp deploy bump).

### 2026-05-23 — Trigger inventory cleanup: deleted 4 duplicate nuclearResetBilling time-based triggers via new cleanupExtraNuclearTriggers RPC
[remoteTriggerStatus](Billing%20dashboard/apps-script/RemoteTrigger.js) audit showed the billing Apps Script project was running **5 separate scheduled `nuclearResetBilling` triggers** on top of the canonical 1× `buildAllBilling` + 1× `dailyDashboardSelfHeal`. Nuclear was never meant to be a standalone cron (the codebase has no `installNuclearResetBillingTrigger` — scheduled-nuclear is the conditional path inside `dailyDashboardSelfHeal`), so the 4 extras were dead weight burning ~4x GHL UrlFetch quota per scheduled fire. New RPC `cleanupExtraNuclearTriggers` in [RemoteTrigger.js](Billing%20dashboard/apps-script/RemoteTrigger.js) (commit `e64beb67`) walks `ScriptApp.getProjectTriggers()`, keeps the first nuclear in iteration order (so any intentional schedule survives), deletes the rest. Idempotent. Shipped at webapp v25 (URL unchanged). Ran live: deleted 4, kept 1. Post-state trigger inventory: 1× nuclear, 1× buildAllBilling, 1× dailyDashboardSelfHeal. Also closes the v24 false-positive in `restoreLostProfileLinks` where a tx row whose embedded source-field label contained "@" was mis-detected as a customer header — v25 picks up the fix (col B detection now also requires col B not be a HYPERLINK formula, since tx rows always are).

### 2026-05-23 — Funnel: embedded the two missing GHL forms on private-lessons + rent-a-sensei
Replaced the hero "Booking Form Coming Soon" placeholder on [funnel/private-lessons.html](funnel/private-lessons.html) (lines 521-531, the `.form-placeholder` block) with Amina's published Private Lesson Booking form (`Cpk2gmz9dcumDiz2KFun`) using the user-supplied iframe embed code. The wrapping `.page-hero-form#register` div and its `.page-hero-form-head` (kicker "Book A Lesson" + h2 "Reserve Your Private Session") stay as-is — only the inner placeholder was swapped for the iframe + `form_embed.js` script. The unused `.form-placeholder*` CSS rules (lines 192-199) are now dead but left in place (per "don't refactor beyond the task"). **Known issue carried over:** Amina admitted in ClickUp thread `90130268726557` that Rule B conditional logic (Instructor = Evenson → Training Type collapses to Boxing only) isn't fully working — user agreed to record a video walking her through using code nodes for conditional logic; the form is live and functional, just the conditional UI may misbehave for certain instructor selections.

On [funnel/rent-a-sensei.html](funnel/rent-a-sensei.html), the page previously had multiple `Book A Lesson` buttons pointing at `#register` but no `#register` element existed — converted the dark navy `.ras-final-cta` "Contact Us to Book" block (lines 719-727) into a `.ras-block#register` form section. New section uses the existing `.ras-block-head` class for the intro (kicker "Ready When You Are" + h2 "Book Your Rent A Sensei Session" + p sub) and embeds Amina's Rent-A-Sensei form (`myEoOLL1SKGv0IvSF4ur`) as a full-width iframe directly inside the block. The "Contact Us to Book" button is gone — the form IS the booking action. All existing `href="#register"` CTAs throughout the page now scroll to a working form. The unused `.ras-final-cta*` CSS rules (lines 291-298) are now dead but left in place.

Iframe sizing strategy on both embeds: inline `style="width:100%;min-height:Xpx;border:none;border-radius:8px;background:#fff;display:block;"` with `X` matching each form's `data-height` (1934 for Private Lessons, 1814 for Rent-A-Sensei) so the iframe has a reasonable footprint before `form_embed.js` loads and resizes it dynamically. No new CSS classes added — just inline styles + existing wrapper classes.

### 2026-05-23 — Nav: removed Spirit Dance from Programs dropdown (redundant with After School), Programs grid now auto-fits 3 cards in a row on desktop
Per user direction "the after school is martial arts and spirit dance already, to remove redundancy". Dropped the Spirit Dance dropdown card from all 7 nav-bearing funnel pages ([home](funnel/home.html), [after-school](funnel/after-school.html), [birthday-parties](funnel/birthday-parties.html), [camps](funnel/camps.html), [private-lessons](funnel/private-lessons.html), [rent-a-sensei](funnel/rent-a-sensei.html), [spirit-dance](funnel/spirit-dance.html)) — same change applied to both the desktop dropdown card AND the mobile menu `<li>`. Programs dropdown now has 3 items (After School, Camps, Rent A Sensei). Changed `.sf-nav-dropdown-grid` CSS rule from `grid-template-columns: repeat(2, 1fr)` → `repeat(auto-fit, minmax(180px, 1fr))` so the grid naturally adapts to its child count: Programs renders 3-cards-in-a-row on desktop (760px menu width / 3 cards ≈ 245px each), Seminars & Events dropdown still has 2 items (Vlad, Birthday Parties) and stays 2-col without any per-dropdown override. Mobile breakpoint at 900px reduces menu width to `calc(100vw - 32px)` and the auto-fit naturally collapses to fewer cols on narrow viewports. **`spirit-dance.html` page itself still exists at the GHL slug `/spirit-dance` and at the GitHub Pages URL** — it's just no longer advertised in nav; if anyone has a direct link they'll still land on it correctly. The After School card's split-thumb (Martial Arts + Spirit Dance side-by-side images with both tags) is already on every nav, making the standalone Spirit Dance card duplicative. Bulk applied via single PowerShell script using exact-string replacement (no regex on the multi-line desktop block); CRLF-aware, all 7 files updated in one pass; the other 5 funnel pages (`summer-camp-scholarship`, `thankyou`, `vlad-seminar`, `waiver`, `waiver-free`) intentionally not touched — they have a simpler/different nav structure outside the 7-page reorg scope.

### 2026-05-23 — Rent-A-Sensei: "Looking for a party?" link now points at the birthday-parties page
On [funnel/rent-a-sensei.html](funnel/rent-a-sensei.html) line ~557, the "This Is Not / A Party or Event Service" card had a CTA pointing at the generic `/#contact` anchor. Swapped the href to `https://www.systemafloyd.com/birthday-parties` (the new GHL slug from the 2026-05-22 nav reorg) and updated the link text from "Looking for a party? Contact us" → "Looking for a party? Book one here" to match the new destination's purpose. Uses `target="_top"` to break out of the GHL iframe wrapper. No other changes to the card.

### 2026-05-23 — Dashboard: by-person attendance modal gains a 5-chip camp/campus filter
Added a chip row above the bulk buttons in the daily attendance modal's by-person mode: `All` · `Upper Paid` · `Lower Paid` · `Upper Free` · `Lower Free`, each labeled with its current count (e.g. "Upper Paid (5)"). Single-select, default `All`, session-persisted across modal opens (resets on page reload — no localStorage). Selecting a filter hides non-matching rows AND scopes the in-list bulk buttons (`All ✓ present` / `All ✗ absent` / `Clear marks`) to only the visible subset, so staff can "mark all Upper Paid present" without touching the other camps. The bottom modal-level Clear button is intentionally NOT filter-scoped (kept as "wipe the whole day" for the same-old reset gesture). Totals at modal bottom and saved Supabase row still count every pick regardless of filter — saved data is filter-agnostic. Checked the 4 source Google Sheets directly (Upper paid `1qejcgNQt3sS_UZ9Gl9Txr8TOocw3LzK5PjPICqnRrGA`, Lower paid `18A_sc917xnxYo3UQ8_cGogqg46Im6qUQlakOC9Oc-Fs`, FREE Higher `1rK4p6jS1xqSf1qNO9-3ljCRzJcUIDF87sNo_UehBWYQ`, FREE Lower `1_659v7by990V4OJMd86nBG-HUN6_AzZNOAPoQN0LMxY`) before designing — confirmed there is no `Camp` column anywhere in the source, the only categorization axes are `type` (summer/free) × `campus` (Upper/Lower), so the 5 chips exhaustively cover what the data carries. The free sheets DO carry a `School` column (Westward Elementary, Northboro, U.B. Kinsey, Pleasant City, Roosevelt) — not surfaced as a secondary filter on this iteration (user declined when offered). Implementation in [dashboard/index.html](dashboard/index.html): added `filterKey` field (`up`/`lp`/`uf`/`lf`) to each person record from `expectedPeopleFor`; new closure var `currentPeopleFilter`; new `visiblePeopleFor` helper; `.att-people .att-filter` CSS (pill chips, active state matches the navy `--sf-blue-700`); chip-click branch added to `peopleEl` delegated click handler; bulk handler swapped to use `visiblePeopleFor` instead of the unfiltered list. Existing dot colors unchanged (Upper Free + Lower Free both still show the cream Free dot, since `bucket` field stayed `f` for compatibility with `countPicks` and the U/L/F totals math). `node --check` of both inline script blocks passes. Not yet browser-tested — worth a click-through before relying on it for a real day.

### 2026-05-23 — Funnel: eliminated all em-dashes (42 total) across all 10 funnel HTML files
Per brand rule (memory `feedback_no_emdashes.md`), did a bulk `[space]—[space]` → `, ` + `[space]&mdash;[space]` → `, ` replacement across every `*.html` under `funnel/`. Covered both user-visible copy (5 JSON-LD pricing-tier names + 1 OG title + 1 image alt-text + 4 body sentences on `vlad-seminar.html`, 2 waitlist banner phrases on `summer-camp-scholarship.html`, 1 JS-string body line on `camps.html`) AND the developer-facing CSS/HTML/JS comments (~28 instances of patterns like `/* HERO — white with blue accents */`) so the codebase reads consistently and there's no risk of a comment string getting promoted into copy later. Two sentences on `vlad-seminar.html` became comma-splices after the mechanical replacement and were hand-refined to use periods instead: (1) [vlad-seminar.html:755](funnel/vlad-seminar.html#L755) "Foundations of breathing, structure, and smooth movement. Set up the principles…", (2) [vlad-seminar.html:885](funnel/vlad-seminar.html#L885) "Stay nearby in West Palm Beach. Both hotels are a short drive away." Verified zero `—` and `&mdash;` remain anywhere in `funnel/`. `dashboard/` (internal staff-facing tooling, not customer-facing copy) was intentionally NOT touched — it still has 8 em-dashes in `index.html` and 12 in `free-camp.html`; flagged for future cleanup if the rule is extended to internal pages. Also gathered all 4 form IDs from Amina's ClickUp comments this turn (Vladimir Vasiliev Seminar `Zu7nHwEILIJnkKyvtnbB`, Balloons `SvXq0KmUb1Ct2AR2t8Yl`, Rent-A-Sensei `myEoOLL1SKGv0IvSF4ur`, Private Lessons `Cpk2gmz9dcumDiz2KFun` — last one has Rule B conditional-logic bug Amina admitted in thread `90130268726557`); funnel-page embed audit showed `private-lessons.html` and `rent-a-sensei.html` are missing form embeds entirely, user said they'll add those manually.

### 2026-05-23 — New billing-dashboard bug-log doc at [billingdashboardbugs.md](billingdashboardbugs.md)
Standalone running log for billing-dashboard bugs (symptom / root cause / fix / deploy state / parent-facing visual impact for each). Seeded with two entries: (1) the +1 week shift bug fixed in `e2a395e9` (active, `nuclearResetBilling` reset still pending), (2) the deferred whitespace-in-legacy-tuition-fingerprints issue from the 2026-05-22 investigation (acceptable to leave alone — canceled rows don't affect balance). Same factual content as the answer I gave the user when they asked "what exactly are we fixing here?" before authorizing the reset. Future billing bugs should land here too, oldest at the bottom.

### 2026-05-23 — Billing reader name-based week resolution (fixes Cyrus +1 week shift) + restored logging on silent row-group failures
Patch to [Billing dashboard/apps-script/BillingFromSheets.js](Billing%20dashboard/apps-script/BillingFromSheets.js). Three changes: (1) new `bfsWeekKey_` / `BFS_WEEK_KEY_MAP` / `bfsResolveWeek_` helpers next to `BFS_WEEK_ORDER` (port of `weekKey_`/`resolveWeek_` from `sheets-snapshot/apps-script/Snapshot.js`), (2) `readRegistrationEnrollments_` rewritten to resolve each tab's week by NAME instead of position, with non-week tabs skipped entirely (no positional fallback — billing under the wrong week is worse than billing nothing), (3) the two silent `catch (e) { /* group quirk */ }` blocks in `appendNewCustomerSection_` and `nuclearResetBilling` now `Logger.log` the customer email + row range + error message so the next round of ungrouped-rows is diagnosable. Resolver sanity-tested against all 12 short-form tab names (`6/15-6/19` → "June 15th-19th", etc.) plus junk inputs (`Notes`/`Lookup`/blank → null). Pre-deploy live audit via Claude Chrome confirmed 0 paid + 0 refund-needed rows in the Dashboard (247 tx rows, 243 owed + 4 unpriced, $48,326.90 balance), so the wrong-week → correct-week reshuffle has zero status-preservation risk — every affected row is `owed` and stays `owed` under the correct week. Deploy: needs `clasp push` from `Billing dashboard/apps-script/` (clasp logged in as `emilio@nilsdigital.com`) then a one-shot `nuclearResetBilling` via the editor or remote-trigger to flush the reshuffle in a single pass. `ACTIVE_INVESTIGATION.md` (since deleted; content now in [billingdashboardbugs.md](billingdashboardbugs.md)) is now superseded by this entry.

### 2026-05-23 — Invited-editor auth fix: GCP project published to Production, unblocking the "+ New manual item" dialog for all invited editors
Resolved same-day. 2SV was enabled on `systemafloydsheets@gmail.com` (Authenticator app added + final "Turn on 2-Step Verification" button clicked — both steps required, adding the authenticator alone doesn't activate 2SV). Claude Chrome then ran the publish flow on GCP project `systema-floyd-billing` (`469572225768`): confirmed correct project (NOT "Nils Digital"), clicked **Publish app** → **Confirm** (did NOT submit for verification). Final state: **Publishing status: In Production**, "Back to testing" button visible. End users hitting the OAuth consent screen will now see "Google hasn't verified this app" instead of Error 403, and can self-bypass via Advanced → "Go to Systema Floyd Billing CLI (unsafe)" → Allow. `SF_INVITED_EDITOR_AUTH.md` (since deleted; the fix is complete and this changelog is the record) header + status sections updated to RESOLVED. Local cleanup completed same-day: end-to-end verified in incognito as `emilionils24@gmail.com` (sidebar opened without Error 403; sidebar `ss_87288ewpo`), smoke-test row at Manual Items row 2 (`dialog-test@example.com`) deleted and tab re-hidden via Claude Chrome (`ss_3497utavy`/`ss_85091h4wy`/`ss_548350a8m`), code-side fix was already on `main` from prior session (`BillingFromSheets.js` manual-item dialog → webapp proxy + `RemoteTrigger.js` `&refresh=1` shipped in `b3bc458b` 2026-05-22), `App_documentation/billing_dashboard.md` checked and contained no manual-item-dialog content needing update.

### 2026-05-23 — Invited-editor auth fix: blocked on Google Cloud's new 2SV/MFA enforcement on `systemafloydsheets@gmail.com`
Claude Chrome ran the publish-to-Production prompt from `SF_INVITED_EDITOR_AUTH.md` (since deleted; the fix is complete and this changelog is the record) and got partway through: confirmed it was on the correct project (`systema-floyd-billing` / `469572225768`, NOT "Nils Digital") and confirmed Publishing status was still **Testing** with a "Publish app" button visible on the Audience page. Every navigation after that redirected to `https://console.cloud.google.com/enable-mfa` with the banner: *"Effective May 20, 2026, Google Cloud has begun to enforce 2-step verification (2SV), also called multi-factor authentication (MFA)."* The PUBLISH button was never clicked. Nothing was changed in either project. Unblock path: enable 2SV on `systemafloydsheets@gmail.com` via https://myaccount.google.com/security, then re-run the same Claude Chrome prompt. Already-issued OAuth tokens (n8n refresh cron, deployed webapps) are not revoked by enabling 2SV — only interactive logins will require the second factor. Granting IAM on the GCP project to `emilio@nilsdigital.com` is not a viable workaround because granting IAM is itself a console action subject to the same MFA wall. `SF_INVITED_EDITOR_AUTH.md` "Where we are now" + "Next step" sections updated with the 2SV enrollment sub-steps and recovery-options caveat.

### 2026-05-22 — Invited-editor auth fix: switched plan from "add Test users" to "publish OAuth consent to Production"
Continuation of the in-flight fix tracked in `SF_INVITED_EDITOR_AUTH.md` (since deleted; the fix is complete and this changelog is the record). The "Next step" section had Claude Chrome harvesting Billing Dashboard editor emails and pasting them into the GCP project's Test users list. Pre-flight check via Google Drive MCP showed the spreadsheet (`1VKwy29-ah7AznrcKs5aGqVKdIuOkCWO6JMUfonas2bM`) is shared as **`anyone` with `writer` role** — link-based access — so there is no discrete editor list to harvest, and unknown signed-in Google accounts will keep hitting Error 403 regardless of who we add. New path: publish the OAuth consent screen for GCP project `469572225768` from Testing → Production. Users will still see the "Google hasn't verified this app" warning but can self-bypass via Advanced → "Go to ... (unsafe)". `SF_INVITED_EDITOR_AUTH.md` "Next step" rewritten with the new Claude Chrome prompt, including a safety check to verify the project name before clicking PUBLISH (avoid hitting the unrelated "Nils Digital" prod project) and an explicit "do NOT submit for verification" guard. Code-side webapp-proxy fix from the prior session is unchanged (still uncommitted, on `main`, smoke-test row `dialog-test@example.com` still at Manual Items row 2 pending cleanup).

### 2026-05-22 — Session resumed from `SESSION_RESUME.md`; handed 5 GHL embed blocks for new nav pages
Resumed work after a `git pull` (commits `17c8a3ee` Georgia/waitlist/scroll-prompt and `46cfc770` nav reorg were already on `origin/main` from the prior session on the other machine). Verified shipped state with the commands in `SESSION_RESUME.md` — all checks passed: Georgia x8 on private-lessons, Waitlist x4 on summer-camp-scholarship, scroll prompt x1 on vlad-seminar, `Seminars &amp; Events` x2 across all 7 funnel pages, zero `Why Us` matches anywhere. Then generated 5 ready-to-paste GHL custom-code embed blocks (one per new nav target: `/private-lessons`, `/spirit-dance`, `/rent-a-sensei`, `/vlad-seminar`, `/birthday-parties`) using the standard full-width breakout + iframe + email-passthrough template from `.claude/CLAUDE.md`, each with a unique iframe id (`sfPrivateLessonsFrame`, `sfSpiritDanceFrame`, `sfRentASenseiFrame`, `sfVladSeminarFrame`, `sfBirthdayPartiesFrame`) pointing at `https://demilio24.github.io/Websites/Tom_Systema_Floyd/funnel/<page>.html`. User has to actually create the GHL pages + paste — Claude can't do that step. Note flagged: the birthday-parties page is technically ready to embed, but the booking form inside it remains a placeholder until Tom responds with prices on Gmail thread `19e512ea0e2e0704`. No code changes committed this turn — only state verification + chat output.

### 2026-05-22 — Investigated "Cyrus missing from billing" — root cause is positional tab indexing, NOT actually missing data
Tom flagged Cyrus bar-or (parent `alexandra.berkley@gmail.com`, registered in PAID Upper for 6/15-6/19, all 5 days checked) as missing from the billing dashboard. **Cyrus is in the dashboard**, but his rows are labeled under `June 22nd-26th` instead of `June 15th-19th`. Same +1-week shift confirmed for Akasha Smith (Aug 3rd-7th → Aug 10th-14th) and Grant Olowin (Lower, Jun 1st-5th → Jun 8th-12th). Root cause: [Billing dashboard/apps-script/BillingFromSheets.js:2399](Billing%20dashboard/apps-script/BillingFromSheets.js#L2399) iterates source-sheet tabs by **position** (`tabs[t]` → `BFS_WEEK_ORDER[t]`), but Tom's spreadsheets have an extra tab shifting the rest. Same bug pattern memory `project_systema_floyd_sheet_tab_names.md` warned about; `Snapshot.js` was fixed via `resolveWeek_` (name-based), `BillingFromSheets.js` never got that fix. Secondary bug: some legacy tuition fingerprints contain whitespace (`5 days` instead of `5-days`) which keeps them perpetually canceled — see Cyrus's canceled tuition row at `sourceRow=23`. **No fix applied yet** — blast radius is significant (will reshuffle many billing rows once corrected). See `ACTIVE_INVESTIGATION.md` (since deleted; content now in [billingdashboardbugs.md](billingdashboardbugs.md)) for full diagnosis, proposed fixes, blast-radius analysis, and the open questions that need user approval before proceeding.

### 2026-05-22 — Pushed Data Issues per-row refactor to main (commit `64df9af7`)
Committed and pushed the per-row Data Issues card refactor. Files: `dashboard/index.html`, `PROJECT.md`. Verified end-to-end with Puppeteer against a local HTTP server (`localhost:8765`, killed after — file:// loading is blocked by Chrome CORS for fetches against `snapshot.json`). Throwaway scratch script `.claude/scratch/snap-data-issues.js` deleted. GitHub Pages picks up within ~1 min.

### 2026-05-22 — Data Issues card: one explicit, labeled, deep-linkable row per bad row
Replaced the per-sheet aggregation in the Data Issues panel ([dashboard/index.html](dashboard/index.html), `renderOpsQuality`) with one explicit `.ops-issue-row` per bad enrollment row. Each row now shows: bold amber field label (e.g. **Days attending**), plain-English why-line ("Mon-Fri checkboxes are all empty for this week"), the precise `SHEET · week · row N` location, and its **own** `Open ↗` button that deep-links straight to that row. No more cramming `rows 4, 6, 6, 9` into one prose line where the button only worked for the first row.

- `byStudent` map renamed `weeks → rows` to match the new semantics (one entry per bad row, not per week). Each entry carries `missingFields` + `kind` so the card can describe each issue individually.
- `FIELD_EXPLAIN` lookup table maps each column name to a plain-English sentence. Unknown fields fall back to `"X is missing"`.
- `describeIssue(w)` handles the three issue kinds: `'name'` ("Student Name column is blank — row exists but has no name"), `'campus'` ("Campus could not be determined…"), `'incomplete'` (joins the per-field explanations with `·`).
- The redundant top-of-card chip strip (the union-of-fields aggregation) was removed — every row now carries its own field label, so the chip strip duplicated information that could mislead (a chip says "Days attending" but staff might not realize it only applies to one of the kid's weeks).
- Card header shows `N issues` (not `N weeks`) since multi-field rows count as one issue each, matching the rendered list.
- New `.ops-issue-row` CSS: amber-tinted background (`#fffaf2`), amber left border (3px `#f4b63a`), bold-amber field label, dark-ink why-line, muted-ink location. Visually scannable like a stack of cards.
- Stress-tested with Austin Shue (6 bad rows, all in Lower sheet, all "Days attending"): each row gets its own clearly-labeled Open ↗ button. Verified end-to-end with Puppeteer against the live dashboard served from `localhost:8765`, screenshots at `.claude/screenshots/data-issues-{new-layout,austin-stress}.png`.

### 2026-05-22 — Pushed dashboard changes to main (commit `30933a16`)
Committed and pushed the three concurrent dashboard changes from today (attendance modal "By person" mode, Supabase sync of per-person picks, Data Issues deep-link with `sourceGid`). Files: `dashboard/index.html`, `sheets-snapshot/apps-script/Snapshot.js`, `App_documentation/dashboard.md`, `PROJECT.md`. GitHub Pages picks up within ~1 min. Other dirty files in the working tree (Billing dashboard JS, untracked Form/ + Summer-2026-rosters/) were intentionally not staged — those are separate in-progress work.

### 2026-05-22 — Dashboard: Data Issues "Open ↗" now deep-links to the offending tab + row
The Data Issues panel groups issues by student name and emits one card per kid, with one "Open ↗" link per source sheet. Before today, that link was `https://docs.google.com/spreadsheets/d/{ID}/edit` — no tab, no row — so clicking it dropped staff on whatever tab Google's default-tab logic picked (usually the first tab). When the offending row was on a later tab, staff would land on the wrong tab, see the data was fine there, and conclude the dashboard was wrong. Akasha Smith's 7/27-7/31 row 2 was the user's reported example (row had empty Mon-Fri checkboxes; staff opened the link, landed on 6/1-6/5 where Akasha did have checkmarks, called the dashboard buggy).

- **`Snapshot.js` change** in [sheets-snapshot/apps-script/Snapshot.js](sheets-snapshot/apps-script/Snapshot.js): every enrollment row now carries `sourceGid: sheet.getSheetId()` alongside the existing `sourceSheetId`, `sourceTabName`, `sourceRow`. Added in `readCampSheet_`, `readFreeSheet_`, and propagated through both summer + free roster dedup blocks. `clasp push -f` from `emilio@nilsdigital.com`'s account at 17:00:33 EDT; the time-driven `pushSnapshotToGitHub` trigger picked it up on its next ~5 min cycle (bot commit `146d9d39` at 18:02 EDT, every roster row in `snapshot.json` now has `sourceGid`). Pinned deployment was NOT re-pushed — that's only used by the manual workflow_dispatch fallback, not the live cron, and updating it requires running from `systemafloydsheets@gmail.com`.
- **Dashboard change** in [dashboard/index.html](dashboard/index.html): `sheetUrlForId(id, gid, row)` now takes 3 args instead of 1. When `gid` is provided, builds `https://docs.google.com/spreadsheets/d/{ID}/edit?gid={gid}#gid={gid}&range=A{row}` — both `?gid=` and `#gid=` are set because Google Sheets requires both in different code paths (router needs the query, the in-app deep-linker reads the fragment). Falls back to the bare `/edit` URL when `gid` is null/undefined, so older snapshots without the field don't break. The data-issues renderer now passes `r.sourceGid` + `r.sourceRow` through to the URL builder.
- **End-to-end verified**: ran the dashboard's `sheetUrlForId` verbatim against the live snapshot; 22 distinct gids across the 4 source sheets × 12 weekly tabs. Opened one of the resulting URLs (Akasha 7/27-7/31, the user's actual example) in Puppeteer against the user's Chrome profile — Google Sheets landed on the `7/27-7/31` tab with cell `A2` selected, no manual tab switching needed.

### 2026-05-22 — Dashboard: per-person attendance now syncs to Supabase
Promoted the per-person ticks added earlier today from local-only (localStorage) to cross-device by adding a new Supabase table + 3 RPCs and rewiring the modal to use them. Picks made on one staff device now appear on every other device.

- **Migration `sf_daily_attendance_people`** applied to project `nroeiabeirifurdaybyo` (Zona Libre, same project as `sf_daily_attendance`). Schema: `(id bigserial, week_label, day_of_week, person_key, person_name, bucket [u|l|f], status [present|absent], marked_by, marked_at)` with unique `(week_label, day_of_week, person_key)`. Row absence = "unmarked" (operator hasn't decided).
- **3 SECURITY DEFINER RPCs** mirroring the totals pattern, gated by the same hardcoded claim_secret as `sf_set_daily_attendance`:
  - `sf_get_daily_attendance_people(p_week_label text default null)` — bulk fetch
  - `sf_set_daily_attendance_person(...)` — single upsert; null/empty status deletes the row (toggle-off)
  - `sf_bulk_set_daily_attendance_people(claim_secret, p_week_label, p_day_of_week, p_status, p_persons jsonb, p_marked_by)` — bulk upsert/clear in one round-trip, used by the All present / All absent / Clear buttons. `p_persons` is `[{key,name,bucket}, …]`.
- **Frontend** in [dashboard/index.html](dashboard/index.html):
  - New state: `state.peopleByWeek` (shape: `{ [week]: { [day]: { [personKey]: { status, name, bucket, markedAt, markedBy } } } }`) + `state.peopleLoading` flag.
  - `loadPeopleAttendance(force)` runs on first roster render alongside `loadAttendance()`, and again on first modal open if not yet loaded. The fetch's `finally` block re-renders the people list if the modal is currently open in by-person mode, so the live UI reflects newly arrived rows.
  - `setPersonPickRpc()` + `bulkSetPeoplePicksRpc()` handle writes.
  - Click handlers are **optimistic**: update local state + DOM first, fire RPC in the background, roll back + show feedback on failure. Snapshot-and-revert pattern using `JSON.parse(JSON.stringify(...))` on the affected cell.
  - `loadPeoplePicks(week, day)` is kept as a sync read accessor — flattens `state.peopleByWeek[week][day]` into the legacy `{key: status}` shape `renderPeopleList`/`countPicks` already speak, so the rest of the UI didn't have to change.
  - `attPeopleKey()`/`savePeoplePicks()` removed (no more localStorage for picks).
  - `sf-att-mode` (last-used mode) stays in localStorage — that's a per-device UX choice, not a shared decision.
- **Totals row** (the original 3 inputs + `sf_set_daily_attendance` RPC) is unchanged. By-person still derives U/L/F counts from ticks at save time and writes them via the totals RPC alongside the per-person writes — so the chart's attendance-mode bars + the roster chip ✓ markers keep working regardless of which mode entered the data.
- **Smoke-tested** the RPCs in psql (set → read → bulk-clear → confirm empty). Frontend JS parses but not yet clicked through in a browser; worth a `/run` before relying on it for a real day.
- **Amina_test/ deleted** earlier this turn at user request — was a scraped GHL form snapshot, untracked in git, and the Stop hook had been firing on it for the past dozen turns. Folder + PROJECT.md gone.

### 2026-05-22 — Dashboard: attendance modal gains a "By person" mode alongside totals
The daily attendance popup (opened from the Mon-Fri chips on each roster week card in [dashboard/index.html](dashboard/index.html)) now supports two entry modes via a pill toggle at the top: **Totals** (the original three Upper/Lower/Free number inputs) and **By person** (a scrollable list of every expected student that day, each with ✓ present / ✗ absent buttons + a colored campus dot). The total line updates live in both modes; by-person also shows the derived U/L/F breakdown. Three bulk buttons at the top of the people list — "All ✓ present", "All ✗ absent", "Clear marks" — for staff who want to start from a baseline and mark exceptions. Clicking the same mark twice toggles it off.

- **Same RPC**, no schema change: both modes save through `sf_set_daily_attendance` (count_upper/lower/free). By-person derives the campus counts from the ticks at save time. `sf_daily_attendance` table is unchanged.
- **Persistence**: per-person ticks live in `localStorage` under `sf-att-people::<week>::<day>`, last-used mode under `sf-att-mode`. Re-opening the same chip restores both the picks and the mode. Existing roster chip ✓ markers still come from saved Supabase totals (so the chip lights up regardless of which mode entered it).
- **Person key**: `${name}|${email}`.toLowerCase() — stable enough across snapshot refreshes for staff-session use; falls back gracefully when email is missing.
- **Clear button** is mode-aware: in by-person it wipes the localStorage picks for that (week, day); in totals it clears the three inputs (original behavior).
- Expected counts come from the same `state.snapshot.roster[week]` walk the modal already used to populate the "Expected: X (U_ · L_ · F_)" line; the helper was extracted to `expectedPeopleFor(week, day)` so the people list and the expected count stay in lockstep.
- Not yet browser-tested — only checked the embedded JS parses. Worth a quick `/run` or click-through before relying on it for a real day.

### 2026-05-18 — Synced local working tree with remote main (merge conflict resolved)
`git pull` brought in ~3,300 remote commits (snapshot bot + the day's billing work I'd missed from a stale local). One conflict in [Billing dashboard/apps-script/RemoteTrigger.js](Billing dashboard/apps-script/RemoteTrigger.js): same line range had my `_rtSampleCanceledRows_` (diagnostic for canceled tx rows) and the remote's `_rtAddManualItem_` (CLI wrapper around the Add Item flow). Both are non-overlapping functions; kept both, placed `_rtAddManualItem_` first then `_rtSampleCanceledRows_`. Pulled the remote's `[^)]+\)` fingerprint regex (multi-word safe — same fix I shipped locally a few sessions back, so they converged). `BillingFromSheets.js` auto-merged without conflicts. Merge commit `844a42d1`. No code semantics changed by the resolution; both pre-merge dispatchers + whitelists already routed `addManualItem` and `sampleCanceledRows` so the case-statements line up with the new function defs without further edits. After this sync, RemoteTrigger.js has all of: form-sheet quick-link chips, Manual Items CLI add, dashboard self-heal install, the full diagnostic suite (`traceCustomer`, `traceAllFreeCampSources`, `listShirtOnlyCustomers`, `dumpRegRow`, `dashboardStats`, `registrationStats`, `sampleCanceledRows`).

### 2026-05-18 — Billing: form-sheet quick-link chips on Dashboard cols K1-O1
Added clickable HYPERLINK chips at the top of the Dashboard tab pointing at the 4 form-submission sheets (Vladimir Seminar, Private Lessons, Rent-A-Sensei, Balloons) + the parent Form Submissions folder. J1 stays free because the existing one-shot actions filter dropdown lives there.

- **`installFormSheetQuickLinks()`** in [BillingFromSheets.js](Billing dashboard/apps-script/BillingFromSheets.js) — walks `FORM_SUBMISSIONS_FOLDER_ID` via `DriveApp`, classifies each Google Sheet against `FORM_SUBMISSION_SHEET_CATEGORIES` (same regex table the reader pipeline uses, so chip wiring stays in lockstep), and writes 5 HYPERLINK formulas. Cells K1-N1 use navy `#143980` chip styling; O1 (folder link) uses dark teal `#0F3634` to read as the parent. Idempotent — re-runs overwrite the same cells. Picks up renamed sheets automatically.
- **Hooked into `buildAllBilling` tail** alongside `sanitizeDashboardCustomerHeaders` + `fixDashboardGroups`, so chips self-heal every 5 min if cleared, and a 5th form sheet added later appears on the next cron fire (as long as it matches one of the 4 existing category regexes — new types need a `FORM_SUBMISSION_SHEET_CATEGORIES` entry).
- **RemoteTrigger whitelist** gained `installFormSheetQuickLinks` (sync=1) so the operator can refresh on demand without waiting for the cron.

### 2026-05-18 — Add Item feature fully shipped + Dashboard healed
End of the Add Item rollout cycle. Status of every piece:
- **Sidebar dialog** — live, verified end-to-end (Chrome extension test, 7/7 passes 2026-05-17). Operator picks contact via autocomplete, fills item / amount / qty / fees / status, submits. Form resets but sidebar persists for batch entry.
- **Hidden `Manual Items` source tab** — created on first dialog submit, hidden via `sh.hideSheet()`. Survives `nuclearResetBilling` because it's a source. Stable UUID fingerprint per row in col K.
- **`#N/A` balance fallout from yesterday's audit-timeout** — resolved. `nuclearResetBilling` re-rendered the Dashboard from sources, rewrote SUMIFS ranges, recreated row groups, and orphan rows like Phoenix Small (col B = `"small"`) disappeared because they're not in any source sheet.
- **Daily 3 AM self-heal trigger** — installed under `emilio@nilsdigital.com` (Workspace 100K UrlFetch quota). Audits Dashboard, runs nuclear reset only when needed, silent on healthy days.
- **Menu state** — `Add Item → + New manual item`, `Audit Dashboard (report only)`, `Repair Dashboard (full rebuild)`. The `Install 3 AM auto-heal trigger` row auto-hides via `buildAddItemMenu_` once `hasDailySelfHealTrigger_` returns true; reappears for any teammate who opens the sheet without their own trigger.

Nothing further required for the Add Item flow. Future operator-visible bugs in this area should be diagnosed first with `Add Item → Audit Dashboard (report only)`, then repaired with `Add Item → Repair Dashboard (full rebuild)` if anything is non-zero.

### 2026-05-18 — Menu: hide self-heal install option once trigger exists
Extracted the Add Item menu builder into `buildAddItemMenu_()` and added `hasDailySelfHealTrigger_()` (scans `ScriptApp.getProjectTriggers()` for a handler matching `dailyDashboardSelfHeal`). The "Install 3 AM auto-heal trigger" row is added conditionally — only when the trigger is NOT yet installed for the current user. After a successful install via `menu_installDailySelfHeal`, the install handler calls `buildAddItemMenu_()` to repaint live (Apps Script's `addToUi()` replaces a menu with the same name), so the option disappears immediately without a sheet reload. The result alert was also updated to mention "the install option will now hide". Triggers are per-user, so a teammate opening the sheet who doesn't have their own trigger would still see the install option — matches the intended "install once per operator" mental model.

### 2026-05-18 — Billing: daily 3 AM auto-heal trigger
Added a daily self-healing trigger that audits the Dashboard at 3 AM Central and runs `nuclearResetBilling` only if something is actually broken — silent on healthy days, email notification when a repair fires.

- **`dailyDashboardSelfHeal`** in [Notifications.js](Billing dashboard/apps-script/Notifications.js) — runs `findUngroupedCustomers_` + `findMalformedCustomerHeaders_` + scans col G for `#N/A` / `#REF` / `#ERROR` / `#VALUE` display values. If any of those three counts is non-zero, calls `nuclearResetBilling`. Logs every run; emails only when (a) a repair happened (info-level) or (b) the function crashed (critical).
- **`installDailyDashboardSelfHealTrigger`** — idempotent installer: wipes any existing trigger on this handler, creates one fresh via `ScriptApp.newTrigger(...).timeBased().everyDays(1).atHour(3).create()`. Run as `emilio@nilsdigital.com` to inherit the Workspace 100K UrlFetch quota.
- **Menu entry** in [Menu.js](Billing dashboard/apps-script/Menu.js): `Add Item → Install 3 AM auto-heal trigger`. One click + a UI alert that echoes the trigger ID + timezone so the operator can confirm it landed.
- **RemoteTrigger whitelist** gained `installDailyDashboardSelfHealTrigger` and `dailyDashboardSelfHeal` (for ad-hoc one-off invocations via curl).

The trigger gets the 30-min execution cap (vs 6-min for menu/UI invocations), so the worst-case nuclear-reset-during-cron lock contention still fits. Bullet behavior: if the 5-min `buildAllBilling` cron holds the lock when 3 AM fires, the self-heal waits up to 2 min (existing `tryLock(120000)` in nuclearResetBilling), then bails with a log line. Next-day attempt will pick up where this one left off. Persistent failure surfaces via the operator-visible `#N/A` balances and the audit menu item.

### 2026-05-18 — Billing: split audit from repair, run repair via background trigger
Yesterday's combined `Audit & Repair Dashboard` menu item hit the **6-min menu-execution cap** on a sheet with many malformed customer headers — the synchronous delete loop plus the trailing `fixDashboardGroups` rebuild blew past the limit, leaving the Dashboard mid-state (partial row deletions invalidated SUMIFS ranges, producing `#N/A` in many Balance cells).

Replaced the single entry with two:
- **`Audit Dashboard (report only)`** — runs `findUngroupedCustomers_` + `findMalformedCustomerHeaders_`, shows a single UI alert with row numbers + names. Read-only, always completes in seconds.
- **`Repair Dashboard (full rebuild)`** — confirms with Yes/No, then schedules `nuclearResetBilling` as a one-time trigger 10s in the future (via `ScriptApp.newTrigger('nuclearResetBilling').timeBased().after(10000).create()`) and returns immediately. The trigger gets the **30-min execution cap** instead of 6-min, so even a 600+ row Dashboard rebuilds cleanly. Apps Script auto-deletes one-time triggers after they fire.

The repair path is also more correct than synchronous deletion: `nuclearResetBilling` rebuilds from source sheets (4 reg + Manual Items + 4 form-submission sheets), so orphan rows like Phoenix Small (col B = `"small"`, not in any source) disappear automatically without needing a targeted delete. Balance SUMIFS formulas are rewritten with correct ranges. Paid / refunded / refund-needed statuses are preserved by fingerprint via the existing prior-status snapshot.

Old `menu_auditAndRepairDashboard` left in [Menu.js](Billing dashboard/apps-script/Menu.js) as orphan code (no menu wiring); can be removed in a later cleanup.

### 2026-05-18 — Billing: one-click Audit & Repair Dashboard
Replaced the standalone `Rebuild row groups` menu entry with a single comprehensive item: **`Add Item → Audit & Repair Dashboard`**. One click runs `fixDashboardGroups`, then two new audit helpers:
- `findUngroupedCustomers_` — walks every customer detected by `readDashboardState_`, checks `getRowGroupDepth(subHeaderRow)`, surfaces anyone still at depth 0 after the rebuild (i.e., where the group-rebuild silently skipped them).
- `findMalformedCustomerHeaders_` — scans for rows where col B is non-empty + non-email AND the next row matches the sub-header pattern (`A=DATE, G=STATUS`). Catches data-import corruption like the Phoenix Small row at 528 (col B was `"small"` — a t-shirt size pasted in place of an email).

The menu hook (`menu_auditAndRepairDashboard`) builds a single combined UI alert. If malformed headers are found, a Yes/No prompt offers in-place deletion via `deleteMalformedCustomerSection_`, which walks down from each bad header, includes the sub-header + any tx rows until the next valid customer header (lookahead via `@` in col B OR next-row sub-header pattern), and deletes in descending row order so indices stay stable across multi-section cleanups. After delete, `fixDashboardGroups` runs once more for a final clean state. All four helpers live in [Menu.js](Billing dashboard/apps-script/Menu.js).

### 2026-05-17 — Billing: 4 new form-submission categories wired into the dashboard
Extended `BillingFromSheets.js` so submissions from the 4 new GHL forms (Vladimir Seminar, Private Lessons, Rent-A-Sensei, Balloons) land on the Dashboard the same way camp enrollments do. Pipeline:

- **Second Drive root constant** `FORM_SUBMISSIONS_FOLDER_ID = '1YnCaA46sLC57w7A3vZf0tEGgZv9aoUxN'` — points to the `Form Submissions` folder in `systemafloydsheets@gmail.com`'s Drive, where the 4 sheets created via Claude Chrome extension live as siblings. `discoverRegistrationSheets_` now walks both roots and merges results; each `walkRoot` call is wrapped in its own try/catch so a permission glitch on one root doesn't kill the other.
- **Four new categories** `seminar`, `private-lessons`, `babysitting`, `decor`, classified by file-name regex against the `FORM_SUBMISSION_SHEET_CATEGORIES` table (sibling sheets — no folder-path hierarchy). `bfsClassifySheet_` extended to take `fileName` as a second arg; old camp / after-school path-based classification kept intact.
- **Four new readers** in `BillingFromSheets.js`: `readSeminarSubmissions_`, `readPrivateLessonSubmissions_`, `readBabysittingSubmissions_`, `readDecorSubmissions_`, with a shared engine `bfsReadSinglePricedColumnForm_` for the two single-priced forms (Vladimir + Private Lessons). All four emit items in the same shape `readManualItems_` produces (price + qty + fingerprint + enrollment), so the downstream diff/reconcile pipeline didn't need any changes. Dispatcher `bfsReadFormSubmissionSheet_` routes by `reg.type`. Pricing comes from `parsePrice()` on each form-option label (e.g. `"Two Day Pass, Early Bird $275"` → `$275`).
- **Babysitting special case** — Rent-A-Sensei has no in-form pricing (Tom quotes manually). Each submission yields one item with `unpriced: true`, `price: 0`, `initialStatus: 'ambiguous'`. Tom edits the Dashboard cell to set the real price + flips status when he sends the quote. Label includes Service Type + Duration + Date for at-a-glance context. No fees applied while unpriced.
- **Multi-row submissions (Balloons)** — one Balloons submission can have up to 9 priced fields (Garland, Add'l Feet, Columns, Arch, Wall, Add-Ons, Delivery Fee, Setup Complexity, Optional Fees). Each non-blank non-"None" priced field becomes its own tx row with fingerprint `decor|<sheet_id>|<row>|<field_key>`, so the team and Emily can mark each line item paid/canceled independently.
- **Fingerprint scheme** — single-priced forms use `<kind>|<sheet_id>|<row_num>`; Balloons uses the field-key suffix above. Stable across rebuilds as long as submission rows aren't deleted or reordered (workflow appends only).
- **buildAllBilling routing** — added an early `FORM_SUBMISSION_TYPES` branch that calls the reader directly and skips the enrollment-pricing two-step. Items already carry their enrollment + fingerprint so they merge into `allItems` identically to manual items. perSheet log line reads `"<sheet>: N submission(s), N tx rows, N unpriced"` for the new types.
- **What's NOT touched**: Pricing tab (its GHL-field-registry auto-population already covers the new offerings), `BFS_NON_BILLABLE_TYPES` (all 4 new types are billable), Dashboard renderer / reconciler (item-shape-agnostic).
- **Companion ClickUp task** `86ahhe0gj` already exists for Amina to build the 4 GHL workflows that write each form submission into its sheet. Once those workflows fire, the billing dashboard picks up new rows within 5 min (cron) or seconds (one-time trigger if invoked).

### 2026-05-17 — Billing: Add Item sidebar dialog + Manual Items source tab + flag-based fee model
Shipped the operator-facing flow for adding ad-hoc line items that don't live on a registration sheet (late fees, private lessons, credits, adjustments). End-to-end pipeline:

- **Manual Items hidden tab** is the new source of truth for manual entries. 11 columns: Email, Student, Item Description, Base Amount, Qty, Apply Processing Fee?, Apply Sales Tax?, Period/Note, Date Added, Initial Status, ID (auto-generated UUID). Stable UUID = stable fingerprint → price edits update the Dashboard row in place; row deletes flip status to `canceled` (or `refund-needed` if `paid`). Survives `nuclearResetBilling` because it's a source, not a Dashboard row. Auto-hidden via `sh.hideSheet()` so the operator's tab strip stays clean — unhide via right-click → Show all hidden sheets when you need to audit.
- **Add Item custom menu** — `Menu.js` `onOpen` re-introduced (was previously gutted). Single sub-item: `+ New manual item` → opens sidebar. Top-bar menu names can't be direct actions per Sheets API, so two-click minimum stays. **Important context preserved in memory `feedback_floyd_billing_ui_distinct.md`:** the gutted `onOpen` only removed *menus*, not row-groups — the customer +/- collapsible groups are a kept feature.
- **Sidebar dialog (`AddManualItem.html`)** — 300px right-edge sidebar with autocomplete contact picker (client-side filter, case-insensitive on name/email/students), student dropdown if the contact has 2+ kids (free-text input for new contacts), Amount + Qty side-by-side, two fee checkboxes (3% processing default on, 7% sales tax default off — uncheck both for raw base-amount), Initial Status radio (Owed/Paid/Canceled). **Contacts are pre-injected via `HtmlTemplate` server-side** so the autocomplete works on the first keystroke (the earlier async-load race was making the first attempt silently empty). After submit the sidebar persists (form resets, contact stays selected) so batch adds skip the menu-click cycle.
- **`applyInlineFees_` refactored to flag-based dispatch** — was `kind`-based (`shirt` got 10%, everything else 3%). Now consults `item.applyProcessing` + `item.applySalesTax` explicitly, falling back to the legacy `kind === 'shirt'` default only when flags are absent. Reg-sheet item behavior unchanged. New zero-fee path (`feeNote: "No tax or processing fee applied..."`) for manual credits/adjustments.
- **Negative amounts allowed** for manual items only (credit / adjustment lines). Validator: `amount === 0 || !isFinite(amount)` rejects; everything else passes. Dashboard balance SUMIFS naturally subtracts.
- **`initialStatus`** is honored on the very first render of a manual fingerprint, then yields to the existing prior-snapshot preservation logic (paid/refunded/refund-needed always preserved). So Initial Status = "Paid" creates the row already paid; later operator edits to the Dashboard pill take precedence on subsequent reconciles.
- **New functions in `BillingFromSheets.js`**: `setupManualItemsTab`, `readManualItems_`, `showAddManualItemDialog`, `manualItemDialog_listContacts`, `manualItemDialog_submit`, `testAddManualItemTemplate` (render-only sanity check). New constant `MANUAL_ITEMS_SHEET_NAME = 'Manual Items'`.
- **RemoteTrigger whitelist** gained `setupManualItemsTab`, `addManualItem` (CLI add via `?fn=addManualItem&email=&label=&amount=...`), `testAddManualItemTemplate`.
- **Splices**: `readManualItems_()` called from both `buildAllBilling` (before sort, after reg-sheet loop) and `nuclearResetBilling_` (before fingerprint dedup) — manual items merge into `allItems` alongside reg-sheet items, identical downstream treatment.

**Caveat — verification gap.** I can `clasp push` to HEAD as `emilio@nilsdigital.com` but cannot create or update Apps Script deployments (Google's same-domain rule, owner is `systemafloydsheets@gmail.com`). Means `clasp run` against the existing v2 deployment doesn't see today's functions, and the v17 webapp deployment also predates them. End-to-end UI verification has to happen from the script owner's account (open the sheet, click `Add Item → + New manual item`). A Claude Chrome extension test prompt was drafted that exercises sidebar open / first-keystroke autocomplete / case-insensitivity / submit-persists / Dashboard landing / full cleanup of the test row from both Manual Items + Dashboard.

**One bug shipped and fixed in the same session:** initial HTML had a JS comment containing the literal string `<?!= ?>`. Apps Script's template engine scans the *entire* file for scriptlet patterns including inside `//` comments — the empty scriptlet threw `SyntaxError: Unexpected token ';'` server-side, blocking the sidebar from opening. Comment removed, only the intended `<?!= contactsJson ?>` scriptlet remains. Lesson logged: never put `<? ?>`-shaped substrings in template-file comments.

**Follow-up patch later same day:** missing +/- row-group toggles spotted on several customer sections (e.g. Natalia Giaquinta at row 522). Root cause: a malformed customer header further down (Phoenix Small at row 528, col B = `"small"` instead of an email — pre-existing data corruption from an earlier import) was making `readDashboardState_`'s `isCustomerHeader` check fail (`b.indexOf('@') !== -1`), so the previous customer's section silently extended across the bad row, and `fixDashboardGroups` couldn't delimit clean boundaries. Patched with a lookahead fallback: if col B is missing `@` but the next row matches the sub-header pattern (`A=DATE, G=STATUS`), the row is still treated as a customer header. Added `Add Item → Rebuild row groups` menu item (`menu_rebuildRowGroups` → `fixDashboardGroups` with a UI alert) for one-click manual repair without waiting for the 5-min cron. Phoenix Small's underlying data corruption is unrelated to today's work and remains as a separate audit follow-up.

**Open follow-ups deferred to next session:**
- True 1-click "Add Item" requires either (a) auto-open sidebar on `onOpen` (rejected — annoying when not adding items), or (b) operator manually inserts an image on the Dashboard with `showAddManualItemDialog` assigned as the click script (one-time setup, persists). Decide later.
- **Supabase migration option** discussed but deferred — could move manual items from the hidden sheet tab to a `floyd_manual_items` Postgres table in the Zona Libre project. Pros: cross-tool queryable from n8n/other dashboards, cleaner mental model. Cons: +200-500ms REST latency per 5-min run, new service-key secret to manage, Supabase outage = manual items missing that run, operator loses easy sheet-edit audit surface. Skip unless there's a real cross-tool need.
- Schema is at 11 columns now (added Qty col E + Initial Status col J between the original 9). Any rows added with an older schema version are NOT auto-migrated — if v1/v2 rows exist (unlikely; user was still testing), wipe and recreate.

### 2026-05-17 — Vlad Seminar landing page shipped + Floyd GHL token chain repaired
Built `funnel/vlad-seminar.html` for the **Tactical Relaxation Festival with Vladimir Vasiliev** (Oct 10 & 11, 2026 at Palm Beach Day Academy). Source materials came from Tom's `Vlad Seminar/` folder: flyer PDF (v2 is current — venue is **Palm Beach Day Academy, 241 Seaview Ave**; v1 had the old "Island Rec Center" address — ignore it), Square page screenshot for pricing, social exports, and the wide composite header graphic. Page follows the existing funnel design system (Oswald/Inter, navy + cream) plus a **gold accent layer** (`--sf-gold #d4a843`) to differentiate the seminar from kid/family programs. Sections: hero with composite graphic + 4-tile meta grid, "About The Master" with credibility pills, 5 core principles (breathing/structure/smooth movement/short work/internal work), 3-card schedule (Friday optional Breathing & Health is gold-ribboned as the special), instructor-renewal strip, 3-tier pricing (Early through Aug 1, Regular, At-the-Door) with `$275/$155 | $355/$200 | $200 only` from the Square screenshot, Friday callout block, venue + 2 hotels, registration iframe, schema.org Event JSON-LD with all 5 offer tiers.

**Token chain repair:** The `acces_token` for "Systema Floyd - Florida" in `public.ghl_tokens` was 2 days stale (n8n cloud hit its monthly execution limit on `s1Z2H8GwdaU7n2kG` so the 3 AM refresh stopped firing). Pulled the GHL marketplace OAuth `client_id` + `client_secret` out of the `2. Get refresh token (Update Agency)` n8n workflow (workflowId `V5mA43CcQhQ0xVj5`), POSTed to `https://services.leadconnectorhq.com/oauth/token` with `grant_type=refresh_token` + `user_type=Location`, got a rotated pair, and wrote both back to Supabase. With the fresh token, uploaded 7 assets to the Florida media library (`8IWtNFlmgJ8bif9DivHT`): hero composite, 4x5 social, OG share, flyer PDF, Systema HQ logo, 9x16 stories, full flyer JPG. Page now references the GHL CDN exclusively; the local `funnel/vlad-seminar-assets/` folder has been removed. **Note for next time the cron is dead:** the manual refresh recipe is documented in this changelog entry — use the same client credentials, target `user_type=Location` for location-scoped tokens, and update Supabase immediately after (refresh tokens rotate on each use; the old one becomes invalid).

**Mobile audit + ship:** Ran a Puppeteer audit across 6 viewports (Galaxy S20 360, iPhone SE 375, iPhone 14 390, Pixel 7 412, iPhone 14 Plus 428, tablet 768) plus desktop 1440. Verified zero horizontal scroll at every width (`document.documentElement.scrollWidth === clientWidth`). Real bugs found + fixed: (1) `.sf-nav-cta` REGISTER pill and `.sf-nav-toggle` hamburger were 42px tall — bumped to 44×44 to meet Apple HIG tap-target minimum; (2) `.sf-footer-social` icons 36→44px; (3) hotel phone links, footer column links, contact rows, and the "nilsdigital.com" credit all got `min-height:44px` + padding for proper finger hit area; (4) `.event-hero` now uses `min-height:100vh; min-height:100svh;` (small viewport units take precedence in modern Safari; old browsers fall back) to avoid the iOS toolbar gap. False positives confirmed harmless (`.event-hero-slash` and `.ribbon` measured as overflowing in raw `getBoundingClientRect()` but are clipped by `overflow:hidden` parents + the body's `overflow-x:hidden` guard). Audit script lives at `.claude/audit-vlad-seminar.js` for future re-runs. Committed + pushed to `main` (commit `3039e467`-ish after rebase over snapshot bot). Live URL: `https://demilio24.github.io/Websites/Tom_Systema_Floyd/funnel/vlad-seminar.html`.

**Hero refactor (form on right, outcome-first copy):** Reworked the hero to match the camps / private-lessons pattern: 2-column grid with copy on the left and a sticky GHL registration form card on the right (form is now above the fold — no scroll needed to register). Composite hero image dropped (it was redundant with the about-section portrait); freed real estate now holds 3 checkmarked outcome bullets between the sub and the meta tiles. Copy rewritten outcome-first: H1 is `Move freely. Think clearly. Stay unshakable.` (third line gold-gradient); sub uses the "so you can…" pattern ("Two days with the world's foremost teacher of Systema, so you can stay tension-free under pressure, move smoothly in any confrontation, and recover instantly from impact — in the dojo, on the street, and in daily life."). Each pillar subtext rewritten the same way ("Reset your nervous system on demand, so you can stay tension-free through impact, fatigue, and confrontation"). Bottom `register-sec` converted from a duplicate iframe into a short "Ready When You Are" CTA banner with a single button that scrolls back to the hero form — avoids double-loading the GHL form and the duplicate `#register` id. `#register` now lives on the hero `<aside class="event-hero-form">`. New responsive rules: 1100px breakpoint stacks the columns (form centered, max 560px wide); 720px breakpoint tightens form padding + iframe min-height. Pushed in commit `47928b79` after rebase over snapshot bot.

### 2026-05-17 — PROJECT.md seeded
Initial seed. Captures both Apps Script pipelines (registrations + billing), folder map, conventions, and all open migration/billing/dashboard threads as of this date.

### 2026-05-07 — Dashboard demo-prep day + registration migration intake
Shipped: auto-refreshing Transactions tab + filters, Pricing Syntax dropdown banner on Logs row 1, manual lunch-modification onEdit feature, legacy registration sheet importer (196 registrations), bulletproof dedup (115 rows / $15,891 removed), `setAllStatusesToOwed`, `normalizeLegacyTxRows`, billing `REPORT.md`. Also drafted 29-kid intake email + handoff note for the registrations dashboard. Reference: `project_systema_floyd_state_snapshot.md`, `context/2026-05-07-dashboard-handoff.md`.
