# Wix → GHL URL Swap Status

**Date checked:** 2026-05-25  
**Status:** BLOCKED — prerequisite files not ready

## What's missing

| File | Status |
|---|---|
| `image_url_map.json` | NOT FOUND |
| `image_inventory.md` | NOT FOUND |

Both files are required before the swap can proceed. The parallel Claude conversation that runs the scrape + GHL upload pipeline has not yet written these files.

## Funnel file state (unchanged)

| File | Wix CDN references remaining |
|---|---|
| `funnel/home.html` | 38 |
| `funnel/home-b.html` | 37 |
| **Total** | **75** |

## Next steps

Once the parallel conversation completes:
1. `image_url_map.json` should appear with 20+ entries mapping Wix URLs → GHL CDN URLs
2. `image_inventory.md` should appear with the deduplicated image list
3. Re-run this swap task — it will detect the files and proceed automatically
